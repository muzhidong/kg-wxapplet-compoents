.select-container {
  width: calc(100% + 32rpx);
  min-height: 88rpx;
  padding: 24rpx 0;
  overflow-x: hidden;
  box-sizing: border-box;
}

.select-container image {
  width: 40rpx;
  height: 40rpx;
  max-width: 40rpx;
  max-height: 40rpx;
  padding-right: 16rpx;
}
